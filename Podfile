source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '10.3'
use_frameworks!
inhibit_all_warnings!

def pods
  
  # Network
  pod 'Alamofire', '~> 4.0.0'
  
  # Code Generator / Analyser
  pod 'SwiftGen', '~> 6.0'
  pod 'Reusable', '~> 4.1'

end

target 'monly-demo' do
  
  # Pods for monly-demo
  pods
  
end
